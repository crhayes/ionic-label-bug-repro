import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'item-sliding-bug-repro',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
